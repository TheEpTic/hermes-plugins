name: hermes-jev-compact
version: 0.1.0
description: "Jev-powered smart tool-prune context engine — scores stale tool calls via conduit2 systemone, falls back to built-in prune."
author: TheEpTic
kind: standalone
provides_context_engine:
  - jev
platforms:
  - linux
  - macos
