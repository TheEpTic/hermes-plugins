name: hermes-ssh
version: 0.4.2
description: "SSH remote operations — run commands and transfer files on registered machines. Requires OpenSSH clients (ssh and sftp)."
author: TheEpTic
kind: standalone
provides_tools:
  - ssh_terminal
  - ssh_transfer
  - ssh_machines
  - ssh_sessions
platforms:
  - linux
  - macos
