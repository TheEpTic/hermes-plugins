name: hermes-ssh
version: 0.3.3
description: "SSH remote execution — run commands on remote machines, track sessions, reuse connections. Requires OpenSSH client (ssh)."
author: TheEpTic
kind: standalone
provides_tools:
  - ssh_terminal
  - ssh_machines
  - ssh_sessions
platforms:
  - linux
  - macos
